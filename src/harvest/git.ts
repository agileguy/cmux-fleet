/**
 * A2 — the repository, which is authoritative (SRD §8.2).
 *
 * Everything here is derived from `git` itself, spawned with argv arrays —
 * never a shell string, because worker-controlled branch names and file paths
 * flow into these commands and a shell would evaluate them.
 */

export {};
