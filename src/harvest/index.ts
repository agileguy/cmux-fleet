/**
 * Harvest assembly and adjudication (SRD §8, §7.3).
 *
 * A1 (envelope, advisory) and A2 (repository, authoritative) are combined here
 * into the `Harvest` object `pifleet artifacts` emits. The envelope may
 * downgrade a verdict; it can never upgrade one.
 */

export {};
