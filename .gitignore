node_modules
.worktrees/
dist/
*.log
.DS_Store
.pifleet/
run-*/
coverage/

# The mount probe's sentinel (MOUNT_PROBE_SENTINEL). `doctor` now probes the
# CHECKOUT as well as the runs and scratch roots, because the relay bind-mounts
# three scripts out of it, and `probeMountVisibility` writes this file into the
# directory it is measuring and removes it in a `finally`. Interrupt the command
# between those two and the file survives — untracked, in the operator's own
# working tree. Ignored so a stray one can never become a commit.
.pifleet-mount-probe

# Federated credential written by google-github-actions/auth. The container
# job moves it out of the workspace and fails if any remain, but a stray file
# must never become a commit.
gha-creds-*.json

# Agent worktrees — transient, created by background agents
.claude/worktrees/

# Local console fleets — the same thing as `fleet.yaml` under a name `--config`
# can point at, naming the operator's own hosts, endpoints and egress
# allowances. `fleet.yaml` ITSELF IS NOW TRACKED, by operator decision
# 2026-09-12: the live config had drifted from `fleet.example.yaml` with no
# diffable record of how, which is the cost the ignore was buying. These two
# stay ignored because they are per-machine scratch configs rather than the
# fleet's one live definition. `fleet.example.yaml` remains the annotated
# reference copy.
fleet-development.yaml
fleet-operations-local.yaml

# ProjectManager run state — per-run working notes, not product. Untracked
# 2026-09-13 before the repository was made public: the `-gabe` document
# carried a wired MAC address, a Tailscale address and the operator's LAN
# layout, none of which is rotatable and none of which any code reads. The
# four variants have no reader at all; `project-manager-state.json` is opened
# by `test/unit/pm-state.test.ts`, which guards on `exists()` and returns
# early, so its absence degrades that check to a no-op rather than a failure.
# The files stay on disk — `git rm --cached` only stops them being published.
.claude/project-manager-state*.json
